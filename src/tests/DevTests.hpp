#define DOCTEST_CONFIG_IMPLEMENT
#include "doctest/doctest.hpp"

// Desc: Tests related to debugging, testing behaviours and overall trying things out.
// Note: there shouldn't be any important tests in here, and if there are some, they
//       should be moved to their appropriate test files.

TEST_CASE("[dev_tests] playground") {
    CHECK(true == true);
}